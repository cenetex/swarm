# Design Philosophy

## Chat-First Interface

This product is chat-first. Every user action should happen inside the chat surface.

- All flows must be triggered in chat with buttons or inline prompts.
- No standalone settings pages, configuration screens, or modal flows.
- If a workflow needs data, collect it with inline chat prompts (buttons, quick replies, secure input).

When in doubt, prefer a chat message + button over a new screen.
