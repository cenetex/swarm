You are worker-016 for SWARM-016.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement deterministic Telegram onboarding step behavior aligned to SWARM-016 states (`pending`, `verified`, `repairable`, `blocked`).
2) Add/extend admin-api onboarding Telegram diagnostics + repair service logic so step status maps deterministically with typed reason codes.
3) Ensure execute/repair operations are idempotent for repeated requests.
4) Integrate Telegram step status and remediation metadata into onboarding orchestrator responses where available in this branch.
5) Keep existing Telegram management endpoints backward-compatible while adding onboarding-step semantics.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Avoid broad refactors in non-Telegram flows.
- If SWARM-013 orchestrator plumbing is not present in-branch, implement reusable Telegram onboarding primitives + integration hooks and record dependency.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-016
worker: worker-016
branch: feat/swarm-016
worktree: /Users/ratimics/develop/aws-swarm-swarm-016
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
