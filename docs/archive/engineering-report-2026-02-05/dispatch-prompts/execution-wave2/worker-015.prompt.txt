You are worker-015 for SWARM-015.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement an onboarding wizard surface in admin-ui, driven by backend onboarding status/actions.
2) Add a dedicated onboarding route for avatar setup (e.g. `/avatars/:id/onboarding`) and wire navigation without breaking existing chat route.
3) Add onboarding API client methods in admin-ui for:
   - GET /onboarding/{avatarId}
   - POST /onboarding/{avatarId}/steps/{stepId}/execute
   - POST /onboarding/{avatarId}/restart
   - POST /onboarding/{avatarId}/steps/{stepId}/skip-optional
4) Render next actions strictly from backend-provided action metadata; avoid client-side transition inference.
5) Add minimal telemetry hooks for funnel stage/action events in the UI layer (no PII payloads).

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Preserve existing admin-ui visual system and avoid large unrelated UI refactors.
- If onboarding backend endpoints are missing in this branch, add resilient UI fallback states and record dependency clearly.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-015
worker: worker-015
branch: feat/swarm-015
worktree: /Users/ratimics/develop/aws-swarm-swarm-015
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
