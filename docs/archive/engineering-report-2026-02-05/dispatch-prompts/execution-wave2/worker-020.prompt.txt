You are worker-020 for SWARM-020.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement runtime feature-flag support for onboarding rollout controls in admin-api (and infra config if required):
   - `onboarding.v2.enabled`
   - `onboarding.v2.rolloutPercent`
   - `onboarding.v2.avatarAllowlist`
   - `onboarding.v2.forceLegacy`
2) Add deterministic cohort assignment helper for new onboarding attempts (sticky assignment key behavior).
3) Add safe fallback/kill-switch behavior so onboarding can be forced to legacy path without deploy changes.
4) Expose minimal diagnostics metadata (flag snapshot + cohort decision) in logs/response context where appropriate for operations.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Keep backward compatibility for existing onboarding behavior when flags are disabled.
- Do not introduce destructive infra changes.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-020
worker: worker-020
branch: feat/swarm-020
worktree: /Users/ratimics/develop/aws-swarm-swarm-020
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
