You are worker-013 for SWARM-013.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement onboarding orchestrator API endpoints in admin-api:
   - GET /onboarding/{avatarId}
   - POST /onboarding/{avatarId}/steps/{stepId}/execute
   - POST /onboarding/{avatarId}/restart
   - POST /onboarding/{avatarId}/steps/{stepId}/skip-optional
2) Add a dedicated onboarding route module under `packages/admin-api/src/handlers/avatar-routes/` and wire it into avatars route dispatch.
3) Add an onboarding orchestrator service under `packages/admin-api/src/services/onboarding/` with deterministic response envelope shape.
4) Use snake_case typed error codes aligned with SWARM-012/SWARM-018 docs.
5) Implement idempotency requirements for mutating endpoints:
   - require `Idempotency-Key`
   - detect replay vs conflict via request fingerprint
   - return deterministic replay responses.
6) Persist onboarding progress server-side (Dynamo-backed preferred; if not feasible, document minimal fallback clearly in code comments and checkpoint).

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Keep scope focused on SWARM-013 API behavior.
- Prefer reusing SWARM-012 state-machine helpers if present in this branch; if missing, add minimal compatible helpers and note the follow-up.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-013
worker: worker-013
branch: feat/swarm-013
worktree: /Users/ratimics/develop/aws-swarm-swarm-013
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
