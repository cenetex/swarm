You are worker-012 for SWARM-012.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement `onboarding_contract_v1` as executable backend state-machine logic.
2) Add a new onboarding state-machine module under `packages/admin-api/src/services/onboarding/`.
3) Include canonical states/events/guards from `docs/engineering-report-2026-02-05/SWARM-012-onboarding-state-machine-contract.md`.
4) Provide deterministic transition validation with typed snake_case error codes.
5) Export reusable helpers for other lanes:
   - create initial onboarding state
   - apply transition/event with guard checks
   - list allowed actions from current state
   - terminal-state and restart handling
6) Wire exports through existing service barrel only where necessary.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Do NOT touch unrelated packages.
- Keep changes compile-safe and minimal for downstream use by SWARM-013/014/017/018.
- If a required dependency is missing, add the smallest reasonable implementation in this lane and record it.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-012
worker: worker-012
branch: feat/swarm-012
worktree: /Users/ratimics/develop/aws-swarm-swarm-012
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
