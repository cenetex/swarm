You are worker-014 for SWARM-014.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement one canonical onboarding auth/account resolver in admin-api service code.
2) Add deterministic outcomes and typed error responses for:
   - allow_continue
   - allow_link
   - require_switch
   - auth/session failures (`session_expired`, `actor_not_authorized`)
3) Enforce explicit link-vs-switch intent handling (no implicit account switching).
4) Integrate resolver into onboarding orchestrator execution path(s) where available in this branch.
5) Ensure wallet + crossmint identity paths can be handled without split-brain account behavior.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Avoid broad auth refactors outside onboarding scope.
- If orchestrator endpoints are not yet present in this branch, still land resolver + integration hooks and record dependency.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-014
worker: worker-014
branch: feat/swarm-014
worktree: /Users/ratimics/develop/aws-swarm-swarm-014
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
