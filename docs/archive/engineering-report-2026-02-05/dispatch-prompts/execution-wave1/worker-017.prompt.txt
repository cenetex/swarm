You are worker-017 for SWARM-017.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement canonical activation readiness report logic (`activation_readiness_v1`) in admin-api service code.
2) Add explicit readiness checks for required activation prerequisites (auth/account, onboarding state, platform requirements, integration health where available).
3) Enforce readiness gate in activation endpoint path(s): block activation when required checks fail.
4) Return deterministic remediation metadata with failures, while preserving backward-compatible `issues[]` when feasible.
5) Add/extend a read endpoint for readiness details if appropriate in existing route structure.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Keep activation behavior backward-compatible unless a contract change is required; if changed, document exactly in checkpoint.
- Coordinate with existing avatar route decomposition patterns.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-017
worker: worker-017
branch: feat/swarm-017
worktree: /Users/ratimics/develop/aws-swarm-swarm-017
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
