You are worker-018 for SWARM-018.

Task type: implementation (code + minimal ticket-doc status updates only if needed).

Primary task:
1) Implement typed onboarding error model utilities under admin-api service code.
2) Add canonical error envelope mapping with:
   - errorType (`validation|transient|dependency|auth|configuration`)
   - snake_case errorCode
   - retryability and retry strategy hints.
3) Implement resume token helper(s) for onboarding attempts (sign/verify/expiry checks).
4) Extend onboarding persistence model with failure history fields suitable for retry/resume decisions.
5) Integrate typed errors and retry/resume metadata into onboarding orchestrator responses where available in this branch.

Constraints:
- Do NOT modify any test files (`*.test.ts`, `*.test.tsx`, `*.test.ts.vitest`) or test configs.
- Keep implementation focused on onboarding flows.
- If orchestrator plumbing is missing in-branch, land reusable primitives and integration hooks, then record dependency.

At the end, output a CHECKPOINT block exactly in this format:

### CHECKPOINT
ticket: SWARM-018
worker: worker-018
branch: feat/swarm-018
worktree: /Users/ratimics/develop/aws-swarm-swarm-018
status: in_progress | review | blocked | merged
latest_commit: <sha-or-UNCOMMITTED>
pr: <url-or-none>
changes: <1-3 lines>
tests_run: <commands and result>
blockers: <none or concrete blocker>
next_step: <single next action>
