# `issues/archive`

Archived local planning artifacts live here.

Current archive sets:

- `open-stale-2026-03-08/` — stale `issues/open/*.yml` mirrors removed from the
  active `issues/open/` directory so the repo matches the GitHub-first workflow.

The remaining hand-maintained mirror directories (`features/`, `bugs/`, `staging/`, `tech-debt/`, `docs/`, `closed/`) and the `issues/open/` directory were retired on 2026-04-17 (see PR for #1380). None of the files carried a `githubIssue` pointer, so nothing links to them. Git history preserves their contents.
