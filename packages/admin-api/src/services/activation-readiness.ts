/**
 * Activation Readiness
 *
 * Thin re-export barrel that preserves the NodeNext import path
 * (`../activation-readiness.js`) used by consumers.
 *
 * Implementation details are split across services/activation/*.ts modules.
 */
export * from './activation/index.js';
